# Инструкция по настройке окружения

## Требования

### Обязательное ПО

| Инструмент | Версия | Назначение |
|------------|--------|------------|
| Xcode | 15.0+ | IDE и компилятор |
| Swift | 6.0+ | Язык разработки |
| XcodeGen | 2.44.1 | Генерация Xcode проекта |
| SwiftGen | 6.6.3 | Генерация ресурсов |
| SwiftLint | 0.63.2 | Линтинг кода |

### Установка через Homebrew

```bash
# Установка инструментов
brew install xcodegen
brew install swiftgen
brew install swiftlint
```

---

## Клонирование проекта

```bash
# Клонирование
git clone https://github.com/your-org/Chat.git
cd Chat
```

---

## Генерация проекта

### Автоматически

```bash
./scripts setup
```

Это выполнит:
1. `xcodegen generate` — генерация `.xcodeproj`
2. `swiftgen` — генерация ресурсов дизайн-системы
3. Откроет Xcode

### Вручную (если нужно)

```bash
# Генерация только Xcode проекта
xcodegen generate

# Генерация ресурсов
swiftgen
```

---

## Открытие проекта

```bash
# Через командную строку
open Chat.xcodeproj

# Или через Xcode
open -a Xcode Chat.xcodeproj
```

---

## Выбор схемы и устройства

### Схемы (Schemes)

| Схема | Назначение |
|-------|------------|
| Chat | Основное приложение |
| ChatTests | Unit-тесты |
| ChatUITests | UI-тесты |

### Устройства

**Симулятор:**
- iPhone 16 (рекомендуется)
- iPhone 16 Pro
- iPhone 16 Pro Max

**Реальное устройство:**
- Saint Celestine (настроено для ship)

---

## Запуск приложения

### Из Xcode

1. Выберите схему **Chat**
2. Выберите устройство/симулятор
3. Нажмите **Cmd + R**

### Из командной строки

```bash
# Сборка
xcodebuild build -scheme Chat -destination 'platform=iOS Simulator,name=iPhone 16'

# Запуск (только для симулятора)
xcrun simctl boot "iPhone 16"
xcrun simctl install booted build/Build/Products/Debug-iphonesimulator/Chat.app
xcrun simctl launch booted com.your-org.Chat
```

---

## Настройка LM Studio

Приложение Chat подключается к LM Studio для LLM.

### Запуск LM Studio

1. Скачайте LM Studio с [lmstudio.ai](https://lmstudio.ai)
2. Запустите приложение
3. Выберите и загрузите модель

### Конфигурация URL

По умолчанию: `http://192.168.1.91:64721`

Изменить можно в настройках приложения.

---

## Запуск тестов

### Все тесты

```bash
./scripts check
```

### Только Unit-тесты

```bash
xcodebuild test -scheme Chat -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:ChatTests
```

### Только UI-тесты

```bash
xcodebuild test -scheme Chat -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:ChatUITests
```

---

## Настройка CI/CD

### Для Ship (деплой на устройство)

```bash
# Настройка беспарольного sudo
./scripts configure-sudo

# Введите пароль при запросе
```

### Требования для Ship

- Настроенный Apple Developer аккаунт
- Привязанное устройство в Xcode
- Code signing identity

---

## Troubleshooting

### Ошибка: "Cannot find project.yml"

```bash
# Убедитесь, что в корне проекта
ls -la project.yml
```

### Ошибка: "SwiftGen not found"

```bash
# Установите SwiftGen
brew install swiftgen

# Или через Mint
mint install SwiftGen/SwiftGen
```

### Ошибка: "No simulators available"

```bash
# Откройте Xcode и добавьте симулятор
# Window → Devices and Simulators → +
```

### Ошибка: SwiftLint warnings

```bash
# Запустите линтер с исправлениями
swiftlint lint --fix
```

---

## Структура проекта после setup

```
Chat/
├── Chat.xcodeproj/      # Сгенерированный проект
├── Chat/
│   ├── App/
│   ├── Features/
│   ├── Services/
│   └── ...
├── ChatTests/
├── ChatUITests/
├── Tools/Scripts/
├── project.yml          # Конфиг XcodeGen
├── swiftgen.yml         # Конфиг SwiftGen
└── .swiftlint.yml       # Конфиг SwiftLint
```

---

## Следующие шаги

1. Запустите `./scripts setup`
2. Откройте `Chat.xcodeproj`
3. Выберите iPhone 16 симулятор
4. Нажмите Cmd + R
5. Приложение запустится! 🎉

Документация: `QWEN.md`, `GUIDELINES.md`, `AI-SELF-REVIEW.md`
