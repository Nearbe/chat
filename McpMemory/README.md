# 🧠 MCP Memory Server для проекта Chat

Кастомный сервер памяти на базе SQLite с поддержкой графов знаний и агентов.

## 📋 Архитектура

```
┌─────────────────┐     ┌──────────────────────┐
│   Mac (Local)   │     │  Windows PC (Worker) │
│                 │     │                      │
│ Continue.dev    │◄───►│ MCP Memory Server    │
│ Qwen3.5-35B     │ HTTP│ Port: 3000           │
└─────────────────┘     └──────────────────────┘
                              │
                        SQLite Database
                        C:\MCP_Memory\data\
```

## 🚀 Быстрый старт (5 команд)

### 1. Установи зависимости на Mac

```bash
cd /Users/nearbe/repositories/Chat/MCP_Memory_Custom
npm install
chmod +x *.sh
```

### 2. Деплой на Windows PC

```bash
./deploy-to-windows.sh
```

**Ожидаемый вывод:**

```
=== Деплой MCP Memory Server на Windows PC ===
[1/5] Создание структуры папок...
✓ C:\MCP_Memory\server created
[2/5] Передача файлов сервера...
✓ server.js transferred
[3/5] Проверка Node.js...
✓ v20.11.0 installed
[4/5] Установка зависимостей npm...
✓ 15 packages installed
[5/5] Запуск MCP Memory Server...
✓ Server started in background
```

### 3. Проверь доступность сервера с Mac

```bash
curl http://192.168.1.107:3000/health
# Ожидаемый ответ:
{"status":"ok","version":"1.0.0","project":"Chat"}
```

### 4. Загрузи данные проекта Chat в память

```bash
./load-project-data.sh
```

**Ожидаемый вывод:**

```
=== Загрузка данных проекта Chat в MCP Memory ===
[1/3] Загрузка узлов агентов...
✓ Узлы загружены (7 nodes)
[2/3] Загрузка связей графа...
✓ Связи загружены (4 edges)
[3/3] Получение статистики графа...
{"total_nodes":7,"total_edges":4,"active_agents":0}
```

### 5. Проверь статистику графа знаний

```bash
curl http://192.168.1.107:3000/stats | jq .
# Ожидаемый ответ:
{
  "total_nodes": 7,
  "total_edges": 4,
  "active_agents": 0,
  "database_path": "C:\MCP_Memory\data"
}
```

## 📚 API Endpoints

### Health Check

```bash
curl http://192.168.1.107:3000/health
# Response: {"status":"ok","version":"1.0.0","project":"Chat"}
```

### Статистика графа знаний

```bash
curl http://192.168.1.107:3000/stats | jq .
# Response:
{
  "total_nodes": 7,
  "total_edges": 4,
  "active_agents": 0,
  "database_path": "C:\MCP_Memory\data"
}
```

### Создание узла графа знаний (агент)

```bash
curl -X POST http://192.168.1.107:3000/nodes \
  -H "Content-Type: application/json" \
  --data '{
    "id": "agent_new",
    "type": "agent",
    "name": "New Agent",
    "content": {
      "role": "Новая роль",
      "trigger_keywords": ["ключевые", "слова"]
    },
    "metadata": {"priority": 1}
  }' | jq .
```

### Получение всех узлов агентов

```bash
curl http://192.168.1.107:3000/nodes?type=agent | jq '.[].name'
# Вывод:
# "CTO / Architecture Lead"
# "Client Developer Lead"
# ...
```

### Создание связи между агентами

```bash
curl -X POST http://192.168.1.107:3000/edges \
  -H "Content-Type: application/json" \
  --data '{
    "source_id": "agent_cto",
    "target_id": "agent_client_developer",
    "relation_type": "manages",
    "weight": 1.0
  }' | jq .
```

### Поиск по графу знаний (RAG)

```bash
curl "http://192.168.1.107:3000/search?q=SwiftUI" | jq '.[].name'
# Возвращает узлы, содержащие "SwiftUI" в content или name
```

### Экспорт графа знаний (для миграции)

```bash
curl http://192.168.1.107:3000/export > backup.json
# Сохраняет весь граф в JSON формат
```

### Импорт графа знаний (из бэкапа)

```bash
curl -X POST http://192.168.1.107:3000/import \
  -H "Content-Type: application/json" \
  --data @backup.json | jq .
# Восстанавливает граф из JSON файла
```

## 🎯 Интеграция с Continue.dev

### Конфигурация MCP Memory Server (`~/.continue/config.json`)

```json
{
  "contextProviders": [
    {
      "name": "mcp-memory",
      "params": {
        "url": "http://192.168.1.107:3000"
      }
    }
  ]
}
```

### Slash Command для работы с памятью

```bash
# Поиск по графу знаний
/mcp-search SwiftUI

# Получение информации об агенте
/mcp-agent client_developer

# Экспорт текущей сессии агента
/mcp-export-session
```

## 📊 Структура базы данных SQLite

### Таблица `nodes` (узлы графа)

| Column     | Type              | Description                      |
|------------|-------------------|----------------------------------|
| id         | TEXT PRIMARY KEY  | Уникальный идентификатор узла    |
| type       | TEXT NOT NULL     | Тип узла (agent, file, decision) |
| name       | TEXT NOT NULL     | Название узла                    |
| content    | TEXT              | JSON-контент узла                |
| metadata   | TEXT DEFAULT '{}' | Дополнительные метаданные        |
| created_at | DATETIME          | Время создания                   |

### Таблица `edges` (связи между узлами)

| Column        | Type             | Description                            |
|---------------|------------------|----------------------------------------|
| id            | TEXT PRIMARY KEY | Уникальный идентификатор связи         |
| source_id     | TEXT NOT NULL    | ID исходного узла                      |
| target_id     | TEXT NOT NULL    | ID целевого узла                       |
| relation_type | TEXT NOT NULL    | Тип связи (manages, collaborates_with) |
| weight        | REAL DEFAULT 1.0 | Вес связи (сильнее = выше вес)         |

### Таблица `sessions` (сессии агентов)

| Column     | Type             | Description                     |
|------------|------------------|---------------------------------|
| id         | TEXT PRIMARY KEY | Уникальный идентификатор сессии |
| agent_id   | TEXT NOT NULL    | ID агента                       |
| context    | TEXT             | JSON-контекст диалога           |
| created_at | DATETIME         | Время создания сессии           |
| updated_at | DATETIME         | Время последнего обновления     |

## 🔧 Troubleshooting

### Проблема: Сервер не запускается на Windows PC

**Решение:**

```powershell
# Проверь, что порт 3000 свободен
netstat -ano | findstr :3000

# Перезапусти сервер вручную
Start-Process node -ArgumentList "C:\MCP_Memory\server\server.js" -WindowStyle Normal
```

### Проблема: Ошибка подключения с Mac

**Решение:**

```bash
# Проверь доступность порта
nc -zv 192.168.1.107 3000

# Если порт закрыт — добавь правило в Firewall Windows:
New-NetFirewallRule -DisplayName 'MCP Memory' -Direction Inbound -Protocol TCP -LocalPort 3000 -Action Allow
```

### Проблема: База данных повреждена

**Решение:**

```bash
# Экспортируй данные перед восстановлением:
curl http://192.168.1.107:3000/export > backup.json

# Удали старую базу и импортируй заново:
rm C:\MCP_Memory\data\memory.db
curl -X POST http://192.168.1.107:3000/import --data @backup.json
```

## 📝 Файлы проекта

| Файл                   | Описание                              |
|------------------------|---------------------------------------|
| `server.js`            | Основной сервер Express + SQLite      |
| `package.json`         | Зависимости (express, better-sqlite3) |
| `deploy-to-windows.sh` | Скрипт деплоя на Windows PC           |
| `load-project-data.sh` | Загрузка данных проекта Chat в память |
| `README.md`            | Эта документация                      |

## 🚀 Следующие шаги

1. ✅ **Установи зависимости:** `npm install`
2. ✅ **Деплой на Windows:** `./deploy-to-windows.sh`
3. ✅ **Загрузи данные проекта:** `./load-project-data.sh`
4. ⏳ **Интегрируй с Continue.dev** (настройка `~/.continue/config.json`)
5. ⏳ **Настрой slash commands** для работы с памятью
6. ⏳ **Добавь векторный поиск** (опционально, через @modelcontextprotocol/server-memory)

---
*Создано: 2026-02-25 | Проект Chat | MCP Memory Server v1.0.0*
