#!/bin/bash

# Скрипт для доставки продукта (Release Build + Deploy)
# Предназначен для передачи готового результата боссу, главному тестеру или заказчику.
# ВНИМАНИЕ: Скрипт запускается ПОСЛЕ check.sh.
# Настроен на выполнение с правами администратора через sudo (если настроено в sudoers).

set -e

# Убедимся, что мы в корне проекта
cd "$(dirname "$0")"

# Проверка на запуск от root (через sudo)
# Если скрипт запущен не от root, он попытается перезапустить себя через sudo
if [ "$EUID" -ne 0 ]; then
  echo "⚖️  Запуск с правами администратора..."
  exec sudo "$0" "$@"
fi

if [ ! -d "Chat.xcodeproj" ]; then
  echo "❌ Ошибка: Проект не подготовлен. Сначала запустите техническую проверку: ./check.sh"
  exit 1
fi

# Переменные конфигурации
DEVICE="Saint Celestine" # Имя устройства (iPhone 16 Pro Max)
# Путь к собранному приложению (используем относительный путь для универсальности)
APP_PATH="./build/Release-iphoneos/Chat.app"

# Проверка на наличие собранного приложения
if [ ! -d "$APP_PATH" ]; then
  echo "❌ Ошибка: Релизная сборка не найдена."
  echo "💡 Запустите техническую проверку для сборки: ./check.sh"
  exit 1
fi

# Установка приложения на устройство
echo "📱 Установка на устройство (Install)..."
xcrun devicectl device install app --device "$DEVICE" "$APP_PATH"

# Запуск приложения на устройстве
echo "🚀 Запуск (Launch)..."
xcrun devicectl device process launch --device "$DEVICE" ru.nearbe.chat

echo "📦 Продукт успешно доставлен на устройство '$DEVICE'!"
echo "✨ Теперь босс и главный тестер могут оценить результат."
