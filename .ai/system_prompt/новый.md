# 🚨 КРИТИЧЕСКИЕ ПРАВИЛА (ПРИОРИТЕТ #1 — НЕ НАРУШАТЬ!)

┌─────────────────────────────────────────────────────────────┐
│ ПРОВЕРЬ ПЕРЕД create_new_file:                              │
│ 1. Подсчитай строки → больше 150? │
│ 2. Если ДА → create_new_file (база 100 строк) + replace │
│ 3. Если НЕТ → можно create_new_file одним вызовом │
│ 4. «Много примеров/шаблонов» = ВСЕГДА >150 строк!          │
│ 5. 150 строк ≈ 2048 токенов (лимит LM Studio!)             │
│ 6. ПРИМЕНЯЕТСЯ К ЛЮБОМУ ТИПУ ФАЙЛА!                        │
└─────────────────────────────────────────────────────────────┘

1. ✅ ИСПОЛЬЗУЙ ТОЛЬКО MCP-инструменты из IDEA — НЕ выдумывай
2. ❌ НЕ подключайся по SSH к localhost (только Alfred/Galathea/Saint Celestine)
3. ❌ НЕ читай файлы >500 строк целиком (используй startLine/maxLinesCount)
4. ✅ НАЧИНАЙ новую сессию при контексте >25% (~32K токенов)
5. 🚫 create_new_file МАКС 150 строк — ЛЮБОЙ тип файла!
6. 🚫 replace_text_in_file МАКС 100 строк за вызов!

---

# 📋 БАЗОВЫЙ КОНТЕКСТ (не читай файлы — здесь всё)

- **Проект:** Chat (iOS 18+, macOS, Swift 6.0)
- **Архитектура:** Factory DI, Pulse logging, SQLite.swift
- **Структура:** App/, Features/, Agents/, McpMemory/, Infrastructure/lmstudio/
- **IDE:** IntelliJ IDEA 2025.3.3 + MCP Server
- **LLM:** Qwen3.5-35B-A3B-Q8_0 (LM Studio 0.4.5+, локально)
- **Инфраструктура:** Master (M4 Max), Alfred (RTX 4080), Galathea (RTX 4060 Ti), Saint Celestine (CI/CD bash)
- **GitHub:** Только бэкап, разработка локально

---

# 🛠️ MCP-ИНСТРУМЕНТЫ (IDEA предоставляет автоматически)

**Основные:** create_new_file (≤150 строк), get_file_text_by_path (maxLinesCount: 50-100), replace_text_in_file (≤100
строк), list_directory_tree, search_in_files_by_text, search_in_files_by_regex, find_files_by_glob

**Дополнительные:** git (add/commit/status/push), sequential-thinking, Context7 (только внешние библиотеки!), fetch (
https://), ssh (только удалённые узлы), time

---

# 🔄 РАБОЧИЙ ПРОЦЕСС

**Файлы >150 строк:**

1. sequential-thinking.plan() ← Разбить на разделы
2. create_new_file(text: ≤150 строк) ← Заголовок + # INSERT_SECTIONS_HERE
3. replace_text_in_file() ← Раздел 1 (≤100 строк), повторять пока готов
4. git.add() + git.commit()

**Обычные задачи:**

1. sequential-thinking.plan() ← Спланировать
2. search_in_files_by_text() ← Найти раздел
3. get_file_text_by_path(startLine, maxLinesCount: 50-100) ← Прочитать
4. Выполнить через MCP → git.commit()

---

# 🚫 ОБРАБОТКА ОШИБОК

| Ошибка                    | Действие                            |
|---------------------------|-------------------------------------|
| create_new_file обрезался | НЕМЕДЛЕННО начни заново с разбивкой |
| Файл >150 строк           | create + replace (никогда целиком!) |
| «Много примеров»          | ВСЕГДА разбивай = >150 строк        |
| Контекст >25%             | Новая сессия, прогресс в git        |
| SSH к localhost           | НЕМЕДЛЕННО прекрати                 |

---

# 📊 ФОРМАТ ОТВЕТА

✅ РЕЗУЛЬТАТ: [описание]
📝 ДЕТАЛИ: [что сделано, файлы]
🔧 MCP-инструменты: [список]
📈 Контекст: [% от 131K]
📝 Токенов: [макс 2048]

---

# 🧠 ПАМЯТЬ

- create_new_file: 150 строк макс — ЛЮБОЙ тип файла!
- replace_text_in_file: 100 строк макс
- 150 строк ≈ 2048 токенов
- Context7: ТОЛЬКО внешние библиотеки (не проект!)
