# Политика безопасности

## Обзор

Безопасность — приоритет для проекта Chat. Этот документ описывает политику безопасности и требования для всех участников.

---

## Защита данных

### Хранение секретов

| Тип данных | Способ хранения | Пример |
|------------|-----------------|--------|
| API токены | **Keychain** | `KeychainHelper.set(key:value:)` |
| Ключи шифрования | **Keychain** | `kSecAttrAccessibleWhenUnlockedThisDeviceOnly` |
| Пароли | **Keychain** | Никогда не в коде |
| API URLs | `AppConfig` | Конфигурация, не в коде |

### Категории секретов

**🔴 Запрещено в коде:**
- Hardcoded API keys
- Пароли пользователей
- Токены авторизации
- Приватные ключи

**🟡 Разрешено с осторожностью:**
- Публичные API endpoints (в конфигурации)
- Имена сервисов
- Версии библиотек

---

## Требования к коду

### Безопасность в Swift

```swift
// ❌ Запрещено — hardcoded secret
let apiKey = "sk-1234567890abcdef"

// ✅ Правильно — из Keychain
let apiKey = KeychainHelper.get(key: "api_key") ?? ""
```

```swift
// ❌ Запрещено — logging sensitive data
print("User password: \(user.password)")

// ✅ Правильно — только в Keychain, без логов
```

### Network Security

- Использовать `https` для всех внешних запросов
- Валидировать SSL certificates
- Не передавать secrets в URL parameters

```swift
// ❌ Запрещено
var components = URLComponents(string: "https://api.example.com?key=\(apiKey)")

// ✅ Правильно — secrets в заголовках
var request = URLRequest(url: url)
request.setValue("Bearer \(token)", forHTTPHeaderField: "Authorization")
```

### Keychain Usage

```swift
// Правильная работа с Keychain
final class KeychainHelper {
    static func set(key: String, value: String) {
        let query: [String: Any] = [
            kSecClass as String: kSecClassGenericPassword,
            kSecAttrAccount as String: key,
            kSecValueData as String: value.data(using: .utf8)!,
            kSecAttrAccessible as String: kSecAttrAccessibleWhenUnlockedThisDeviceOnly
        ]
        SecItemDelete(query as CFDictionary)
        SecItemAdd(query as CFDictionary, nil)
    }
}
```

---

## Уязвимости

### Как сообщить об уязвимости

1. **Не создавайте public issue** для security проблем
2. Напишите **privately** владельцу проекта
3. Опишите:
   - Описание уязвимости
   - Шаги воспроизведения
   - Potential impact
   - Предложенное решение (опционально)

### Принятые уязвимости

- SQL Injection → Используйте SwiftData/ORM с параметризированными запросами
- XSS → SwiftUI автоматически экранирует
- CSRF → Токены в заголовках
- Data leaks → Keychain для secrets

---

## Тестирование безопасности

### Что проверять

- [ ] Нет hardcoded secrets в коде
- [ ] Secrets загружаются из Keychain
- [ ] Sensitive data не логируется
- [ ] HTTPS используется для всех запросов
- [ ] SSL validation включена
- [ ] Keychain access level корректный

### Инструменты

```bash
# Поиск secrets в коде
grep -r "sk-" --include="*.swift" .
grep -r "password" --include="*.swift" .
grep -r "api_key" --include="*.swift" .

# Или использовать secret-scanner навык
```

---

## Политика обновлений

| Тип | Срок |
|-----|------|
| Критические уязвимости | 24 часа |
| Высокий приоритет | 7 дней |
| Средний приоритет | 30 дней |
| Низкий приоритет | 90 дней |

---

## Контакты

Для вопросов безопасности: связаться с владельцем проекта_privately.
