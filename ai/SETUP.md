# Инструкция по настройке окружения

## Требования

### Обязательное ПО

| Инструмент | Версия | Назначение                                        |
|------------|--------|---------------------------------------------------|
| Xcode      | 15.0+  | IDE и компилятор                                  |
| Swift      | 6.0+   | Язык разработки                                   |
| Homebrew   | latest | Менеджер пакетов (для автоустановки инструментов) |

**Примечание:** XcodeGen, SwiftGen и SwiftLint устанавливаются автоматически при первом запуске `./scripts setup`.

---

## Клонирование проекта

```bash
# Клонирование
git clone https://github.com/your-org/Chat.git
cd Chat
```

### Разворачивание окружения

```bash
./deploy.sh
```

Этот скрипт:

1. Проверит/установит XcodeGen, SwiftGen, SwiftLint через Homebrew
2. Соберёт бинарник скриптов
3. Создаст системные команды Setup, Check, Ship и др. в `~/bin`

**После разворачивания** добавьте ~/bin в PATH если ещё не добавлен:

```bash
echo 'export PATH=$PATH:~/bin' >> ~/.zshrc
source ~/.zshrc
```

---

## Генерация проекта

### Автоматически

```bash
./scripts setup
```

Это выполнит:

1. `xcodegen generate` — генерация `.xcodeproj`
2. `swiftgen` — генерация ресурсов дизайн-системы
3. Откроет Xcode

### Вручную (если нужно)

```bash
# Генерация только Xcode проекта
xcodegen generate

# Генерация ресурсов
swiftgen
```

---

## Открытие проекта

```bash
# Через командную строку
open Chat.xcodeproj

# Или через Xcode
open -a Xcode Chat.xcodeproj
```

---

## Выбор схемы и устройства

### Схемы (Schemes)

| Схема       | Назначение          |
|-------------|---------------------|
| Chat        | Основное приложение |
| ChatTests   | Unit-тесты          |
| ChatUITests | UI-тесты            |

### Устройства

**Симулятор:**

- iPhone 16 (рекомендуется)
- iPhone 16 Pro
- iPhone 16 Pro Max

**Реальное устройство:**

- Saint Celestine (настроено для ship)

---

## Запуск приложения

### Из Xcode

1. Выберите схему **Chat**
2. Выберите устройство/симулятор
3. Нажмите **Cmd + R**

### Из командной строки

```bash
# Сборка
xcodebuild build -scheme Chat -destination 'platform=iOS Simulator,name=iPhone 16'

# Запуск (только для симулятора)
xcrun simctl boot "iPhone 16"
xcrun simctl install booted build/Build/Products/Debug-iphonesimulator/Chat.app
xcrun simctl launch booted com.your-org.Chat
```

---

## Запуск скриптов (check, setup, ship)

### Установка (однократно)

Для удобного доступа к скриптам из любой директории добавьте обёртку в PATH:

```bash
ln -s /Users/nearbe/repositories/Chat/chat-scripts.sh /usr/local/bin/chat-scripts
```

После этого команды `chat-scripts check`, `chat-scripts setup`, `chat-scripts ship` будут доступны глобально.

### Через Run Configuration (рекомендуется)

В IntelliJ IDEA / WebStorm настроены Run Configurations для запуска скриптов:

1. **Run** → **Edit Configurations...**
2. Выберите нужную конфигурацию:
   - `scripts — check` — линтинг + сборка + тесты + пуш
   - `scripts — setup` — генерация проекта
   - `scripts — ship` — релиз-сборка + деплой на устройство
3. Нажмите **Run** (или **Debug** для отладки)

**Горячая клавиша:** Выберите конфигурацию в выпадающем списке сверху и нажмите **⌘ + R**

### Через терминал

```bash
# После установки (см. выше)
chat-scripts check
chat-scripts setup
chat-scripts ship

# Или напрямую
./chat-scripts.sh check
./chat-scripts.sh setup
./chat-scripts.sh ship
```

### Автоматическая сборка

Если бинарник скриптов отсутствует (например, после очистки проекта `swift build --clean`), обёртка автоматически
выполнит сборку перед запуском команды. Ручная пересборка не требуется.

---

## Настройка LM Studio

Приложение Chat подключается к LM Studio для LLM.

### Запуск LM Studio

1. Скачайте LM Studio с [lmstudio.ai](https://lmstudio.ai)
2. Запустите приложение
3. Выберите и загрузите модель

### Конфигурация URL

По умолчанию: `http://192.168.1.91:64721`

Изменить можно в настройках приложения.

---

## Авторизация и безопасность

**Важно:** 
- Авторизация привязана к устройству через `DeviceConfiguration` и `DeviceIdentity`
- Токены хранятся **только** в Keychain через `KeychainHelper`
- Не сохраняйте секреты в `UserDefaults`

---

## Запуск тестов

### Все тесты

```bash
./scripts check
```

### Только Unit-тесты

```bash
xcodebuild test -scheme Chat -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:ChatTests
```

### Только UI-тесты

```bash
xcodebuild test -scheme Chat -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:ChatUITests
```

---

## Настройка CI/CD

### Важно: Локальный CI/CD

Весь CI/CD работает **локально** на машине **Saint Celestine**:

| Компонент       | Где выполняется                   |
|-----------------|-----------------------------------|
| `scripts check` | Локально (линтинг, сборка, тесты) |
| `scripts setup` | Локально (XcodeGen, SwiftGen)     |
| `scripts ship`  | Локально (релиз-сборка, деплой)   |

**GitHub используется только как backup** — чтобы ничего не пропало. Никаких GitHub Actions, webhooks или облачного CI.

### Для Ship (деплой на устройство)

```bash
# Настройка беспарольного sudo
./scripts configure-sudo

# Введите пароль при запросе
```

### Требования для Ship

- Настроенный Apple Developer аккаунт
- Привязанное устройство в Xcode
- Code signing identity

---

## Troubleshooting

### Ошибка: "Cannot find project.yml"

```bash
# Убедитесь, что в корне проекта
ls -la project.yml
```

### Ошибка: "SwiftGen not found"

```bash
# Запустите setup - он установит все зависимости автоматически
./scripts setup
```

### Ошибка: "No simulators available"

```bash
# Откройте Xcode и добавьте симулятор
# Window → Devices and Simulators → +
```

### Ошибка: SwiftLint warnings

```bash
# Запустите линтер с исправлениями
swiftlint lint --fix
```

---

## Структура проекта после setup

```
Chat/
├── Chat.xcodeproj/      # Сгенерированный проект
├── Chat/
│   ├── App/
│   ├── Features/
│   ├── Services/
│   └── ...
├── ChatTests/
├── ChatUITests/
├── Tools/Scripts/
├── project.yml          # Конфиг XcodeGen
├── swiftgen.yml         # Конфиг SwiftGen
└── .swiftlint.yml       # Конфиг SwiftLint
```

---

## Настройка JetBrains AI Assistant

### AI Self-Review (обязательно)

Для использования AI-powered code review перед коммитом:

1. **Settings** → **Tools** → **AI Assistant** → **Project Settings**
2. В поле **Path to rules for AI Self-Review** указать:
   ```
   /Users/nearbe/repositories/Chat/AI-SELF-REVIEW.md
   ```
3. Нажать **Apply** → **OK**

**Проверка:**

- Откройте Commit tool window: **Alt + 0** (Windows/Linux) или **⌘ + 0** (macOS)
- Выберите файлы → нажмите **Self-Review with AI**

Подробности: `AI-SELF-REVIEW.md`

---

## Регистрация агентов (AI-команда)

При `./scripts setup` автоматически регистрируются AI-агенты из папки `Agents/` в систему навыков Qwen.

### Как это работает

1. `./scripts setup` сканирует папку `Agents/`
2. Для каждого агента с `SKILL.md` создаётся навык в `~/.qwen/skills/`
3. Qwen автоматически подхватывает навыки при старте

### Доступные агенты

| Агент            | Описание                  |
|------------------|---------------------------|
| cto              | Технический директор      |
| client-lead      | Лид клиентской разработки |
| client-developer | iOS-разработчик           |
| server-lead      | Лид серверной разработки  |
| server-developer | Серверный разработчик     |
| ...              | и др. (см. `Agents/`)     |

### Ручная перерегистрация

```bash
./scripts register-agents
```

---

## Следующие шаги

1. Запустите `./scripts setup`
2. Откройте `Chat.xcodeproj`
3. Выберите iPhone 16 симулятор
4. Нажмите Cmd + R
5. Приложение запустится! 🎉

Документация: `QWEN.md`, `GUIDELINES.md`, `AGENTS.md`, `SETUP.md`, `TESTING.md`, `VERSIONING.md`
