# 🏗️ Архитектура проекта Chat

> **Версия:** 2.0  
> **Дата обновления:** 2026-02-25  
> **Архитектурный паттерн:** MVVM (Model-View-ViewModel)

---

## 📐 Общая архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                      PRESENTATION LAYER                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │  Views   │  │ Modifiers│  │  Sheets  │  │  Alerts  │   │
│  │ (SwiftUI)│  │          │  │          │  │          │   │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘   │
│       │             │             │             │           │
│       └─────────────┴───────┬─────┴─────────────┘           │
│                             │                                │
├─────────────────────────────┼────────────────────────────────┤
│                    DOMAIN LAYER                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │              ViewModels (ObservableObject)           │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐          │   │
│  │  │ ChatVM   │  │ Settings │  │  Auth    │          │   │
│  │  │          │  │   VM     │  │   VM     │          │   │
│  │  └────┬─────┘  └────┬─────┘  └────┬─────┘           │   │
│  └───────┼──────────────┼──────────────┼────────────────┘   │
│          │              │              │                     │
├──────────┴──────────────┴──────────────┴────────────────────┤
│                   DATA LAYER                                  │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │ SwiftData   │  │  Network    │  │   Security  │         │
│  │  Context    │  │  Service    │  │  Manager    │         │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘         │
│         │                │                │                 │
├─────────┴────────────────┴────────────────┴─────────────────┤
│                    EXTERNAL INTEGRATIONS                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │ LM Studio│  │ Ollama   │  │ OpenAI   │  │  CloudKit │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎯 Принципы архитектуры

### 1. **MVVM (Model-View-ViewModel)**

```
View (SwiftUI) ←→ ViewModel (ObservableObject) ←→ Model (SwiftData)
     │                    │                          │
┌─────────────┐    ┌──────────────┐      ┌─────────────┐
│ Declarative │      │ @Published   │       │ Entity      │
│ UI          │◄─────│ State        │◄──────│ SwiftData   │
│             │      │ Computed     │       │ Models      │
└─────────────┘      └──────────────┘       └─────────────┘
```

**Преимущества:**

- ✅ Чистое разделение ответственности
- ✅ Легкое тестирование ViewModels
- ✅ SwiftUI реактивность через @Published
- ✅ Dependency Injection для сервисов

### 2. **Clean Architecture (3 слоя)**

| Слой             | Ответственность               | Технологии            |
|------------------|-------------------------------|-----------------------|
| **Presentation** | UI, навигация, взаимодействия | SwiftUI, Combine      |
| **Domain**       | Бизнес-логика, Use Cases      | Swift, protocols      |
| **Data**         | Persistence, Networking       | SwiftData, URLSession |

### 3. **Reactive State Management**

```swift
// ViewModel с реактивным состоянием
class ChatViewModel: ObservableObject {
    @Published var messages: [Message] = []
    @Published var isLoading: Bool = false
    @Published var error: String?
    
    // Computed properties для UI
    var isEmpty: Bool { messages.isEmpty }
    var lastMessage: Message? { messages.last }
}
```

---

## 📦 Структура проекта

```
Chat/
├── App/                          # Приложение и навигация
│   ├── ChatApp.swift             # Entry point
│   ├── ContentView.swift         # Root view
│   └── Navigation/
│       ├── AppNavigator.swift    # Main navigation logic
│       └── Routes.swift          # Navigation paths enum
│
├── Features/                       # Фичи как независимые модули
│   ├── Chat/
│   │   ├── View/
│   │   │   ├── ChatView.swift    # Основной экран чата
│   │   │   └── Components/
│   │   │       ├── MessageRow.swift
│   │   │       └── InputBar.swift
│   │   ├── ViewModel/
│   │   │   └── ChatViewModel.swift
│   │   └── Model/
│   │       └── ChatDomainModel.swift
│
├── Core/                           # Общие компоненты
│   ├── Networking/
│   │   ├── APIClient.swift        # URLSession wrapper
│   │   ├── SSEStream.swift        # Server-Sent Events
│   │   └── Endpoints.swift        # API paths enum
│   |
│   ├── Data/
│   │   ├── SwiftData/
│   │   │   ├── Database.swift     # ModelContainer
│   │   │   ├── MessageModel.swift # Entity
│   │   │   └── ChatModel.swift    # Entity
│   │   └── Repositories/
│   │       ├── MessageRepository.swift
│   │       └── ChatRepository.swift
│
├── Design/                         # Дизайн система
│   ├── Colors.xcassets
│   ├── Fonts/
│   ├── Components/
│   │   ├── ButtonStyles.swift
│   │   ├── TextFieldStyles.swift
│   │   └── CardStyles.swift
│   └── Themes/
│       ├── LightTheme.swift
│       └── DarkTheme.swift
│
└── Resources/                      # Ассеты
    ├── Assets.xcassets
    ├── Localizable.strings
    └── Info.plist
```

---

## 🔄 Потоки данных

### 1. **Чат с AI (SSE Streaming)**

```swift
// Flow: View → ViewModel → Network Service → SSE Stream

ChatView
   ↓
sendMessage(text)
   ↓
ChatViewModel.sendMessage()
   ↓
APIClient.sseStream(endpoint: "/chat")
   ↓
URLSession.dataTaskPublisher()
   ↓
SSEParser.streamEvents()
   ↓
@Published var messages: [Message]
   ↓
View updates (SwiftUI reactive)
```

### 2. **Сохранение в SwiftData**

```swift
// Flow: ViewModel → Repository → SwiftData Context

ChatViewModel
   ↓
saveMessage(message)
   ↓
MessageRepository.insert()
   ↓
do { try container.performBatch {} }
   ↓
SwiftData ModelContainer
   ↓
@Query var messages: [Message]
   ↓
View automatically updates
```

---

## 🔐 Безопасность архитектуры

| Уровень       | Механизм           | Реализация                              |
|---------------|--------------------|-----------------------------------------|
| **UI**        | Input validation   | SwiftUI `TextField` + custom validators |
| **Network**   | HTTPS only         | URLSession with TLS 1.3                 |
| **Auth**      | JWT tokens         | Secure storage в Keychain               |
| **Data**      | Encryption at rest | AES-256 для чувствительных данных       |
| **Biometric** | Face ID/Touch ID   | LocalAuthentication framework           |

---

## 📊 Производительность

### Оптимизации:

1. **Lazy loading чатов** — загрузка только последних 50 сообщений
2. **Image caching** — NSCache для аватарок и медиа
3. **Background sync** — CloudKit background fetch
4. **SSE connection pooling** — повторное использование URLSession
5. **SwiftData indexing** — индексы для частых запросов

---

## 🔗 Связанные модули

- **[01_project_overview.md](./01_project_overview.md)** — обзор проекта
- **[04_data_models.md](./04_data_models.md)** — SwiftData схемы
- **[05_api_integration.md](./05_api_integration.md)** — API протоколы
- **[07_state_management.md](./07_state_management.md)** — управление состоянием

---

> **Архитектор:** Team Nearbe  
> **Версия архитектуры:** 2.0 (2026-02-25)

</parameter>}}] | end_of_message
