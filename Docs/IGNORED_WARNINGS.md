# Реестр исключений и системных предупреждений

Реестр допустимых предупреждений (Xcode, Simulator, OS) и программных исключений (SwiftLint, ProjectChecker), проанализированных для автоматической проверки.

## 🛠️ Системные предупреждения (Xcode/Build)

| № | Паттерн | Причина | Обоснование |
|:--|:---|:---|:---|
| 1 | `Metadata extraction skipped. No AppIntents.framework dependency found.` | Xcode ищет AppIntents, даже если они не используются. | Особенность Xcode 15/16. Фикс требует лишней зависимости. |
| 2 | `Ignoring --strip-bitcode because --sign was not passed` | Сборка без подписи провоцирует попытку стриппинга. | Отключено в CI для скорости. Не влияет на тесты. |

## 🔍 Программные исключения (ProjectChecker)

Исключения в кастомном инструменте проверки структуры проекта (`ProjectChecker.swift`), разделенные по типам проверок.

### 📂 Исключенные папки
Папки, которые полностью игнорируются при сканировании файлов.

| № | Папка | Причина | Обоснование |
|:--|:---|:---|:---|
| 1 | `Design/Generated` | Сгенерированный код. | Автогенерация SwiftGen, не требует ручной правки. |
| 2 | `Resources` | Статические ресурсы. | Содержит медиафайлы и JSON, не анализируется. |
| 3 | `Tools/Scripts` | Служебные скрипты. | Вспомогательный инструментарий разработки. |
| 4 | `Chat.xcodeproj` | Проектная папка. | Системная структура Xcode проекта. |
| 5 | `.build` | Артефакты сборки SwiftPM. | Временные файлы компиляции пакетов. |
| 6 | `build/` | Системная папка сборки. | Локальные артефакты компиляции. |
| 7 | `chatbuild/` | Кастомная папка сборки. | Артефакты сборки CI/CD скриптов. |
| 8 | `ChatTests` | Юнит-тесты. | Тестовый код не требует строгой документации. |
| 9 | `ChatUITests` | UI-тесты. | UI-тесты не требуют строгой документации. |

### 🏷️ Игнорируемые символы
Символы (классы, методы, свойства), для которых не требуется наличие Docstring.

| № | Символ | Причина | Обоснование |
|:--|:---|:---|:---|
| 1 | `CodingKeys` | Стандартный Swift Boilerplate. | Очевидный стандартный enum для Codable. |
| 2 | `makeUIViewController` | UIViewControllerRepresentable. | Стандартный метод протокола SwiftUI. |
| 3 | `updateUIViewController` | UIViewControllerRepresentable. | Стандартный метод протокола SwiftUI. |
| 4 | `makeCoordinator` | UIViewControllerRepresentable. | Стандартный метод протокола SwiftUI. |
| 5 | `makeBody` | SwiftUI View. | Стандартный метод протокола SwiftUI. |
| 6 | `body` | SwiftUI View. | Основное свойство протокола SwiftUI View. |
| 7 | `id` | Identifiable. | Обязательное свойство протокола Identifiable. |
| 8 | `hash` | Hashable. | Обязательное свойство протокола Hashable. |

### 🔑 Технические ключевые слова
Слова и префиксы, допустимые в англоязычном написании внутри русскоязычных Docstring.

| № | Ключевое слово | Причина | Обоснование |
|:--|:---|:---|:---|
| 1 | `///` | Символ документации. | Технический маркер начала комментария (Docstring). |
| 2 | `TODO:` | Метка задачи. | Временная заметка разработчика. |
| 3 | `FIXME:` | Метка исправления. | Признанная ошибка, стоящая в очереди на исправление. |
| 4 | `NOTE:` | Метка примечания. | Пояснительная информация в коде. |
| 5 | `JSON` | Технический термин. | Стандартная аббревиатура формата данных. |
| 6 | `http` | Ссылка в документации. | Префикс протокола передачи данных. |
| 7 | `- Parameters:` | Параметры Docstring. | Стандартный заголовок параметров Apple Doc. |
| 8 | `- Returns:` | Возвращаемое значение Docstring. | Стандартный заголовок результата Apple Doc. |
| 9 | `- Throws:` | Ошибки в Docstring. | Стандартный заголовок ошибок Apple Doc. |

### 📝 Разрешенные контексты print()
Токены и пути, наличие которых в строке с `print()` разрешает его использование.

| № | Контекст | Причина | Обоснование |
|:--|:---|:---|:---|
| 1 | `Tools/Scripts` | Разрешено для CLI инструментов. | Прямой вывод в консоль необходим в скриптах. |
| 2 | `#Preview` | Разрешено для быстрой отладки в SwiftUI. | Временный вывод для отладки в превью-холсте. |
| 3 | `Logger` | Логирование через обертку. | Прокси-метод для системы Pulse. |
| 4 | `metrics.csv` | Запись метрик. | Прямой вывод данных в файл сбора метрик. |

### 📏 Порог коротких строк
Условие для игнорирования проверки языка для очень коротких технических описаний.

| № | Текст | Причина | Обоснование |
|:--|:---|:---|:---|
| 1 | `<15` | Короткие технические строки (ID, 4pt и т.д.). | Минимальная длина текста для смысловой нагрузки. |

## 🧹 Исключения SwiftLint (.swiftlint.yml)

Список исключенных путей в статическом анализаторе SwiftLint.

| № | Тип | Имя / Паттерн | Причина | Обоснование |
|:--|:---|:---|:---|:---|
| 1 | Папка | `Chat.xcodeproj` | Служебная папка Xcode. | Содержит конфигурации проекта в XML, не требует анализа. |
| 2 | Папка | `Resources` | Статические ресурсы. | Содержит ассеты и JSON, не анализируется линтером. |
| 3 | Папка | `Design/Generated` | Сгенерированный код. | Генерируется SwiftGen, может не соответствовать стилю. |
| 4 | Папка | `Tools/Scripts/.build` | Артефакты SwiftPM. | Временные файлы компиляции вспомогательных скриптов. |
| 5 | Папка | `.build` | Артефакты SwiftPM. | Временные файлы компиляции основного проекта. |

## 🏗️ Исключения XcodeGen (project.yml)

Конфигурация исключений и внешних зависимостей в генераторе проекта.

| № | Тип | Имя / Паттерн | Причина | Обоснование |
|:--|:---|:---|:---|:---|
| 1 | Папка | `Tools/Scripts` | Внешний пакет. | Вынесен в отдельный локальный SPM-пакет (Scripts). |
| 2 | Папка | `.build` | Артефакты сборки. | Системная папка компиляции, игнорируется при сборке. |

## 📐 Конфигурации инструментов

Где настраиваются другие исключения в проекте:

- **SwiftLint**: `.swiftlint.yml` (секция `excluded`). Игнорируются сгенерированные файлы и внешние зависимости.
- **XcodeGen**: `project.yml`. Настройка структуры проекта и исключений из сборки.
- **Скрипты**: `Check.swift` — загружает список разрешенных предупреждений из таблицы выше.

---

## 📝 Правила добавления
1. Добавить новую строку в соответствующую таблицу.
2. Для системных предупреждений: ничего менять в коде не нужно — `Check.swift` подгружает паттерны из этой таблицы.
3. Для проверок структуры: ничего менять в коде не нужно — `ProjectChecker` подгружает исключения из этой таблицы.
